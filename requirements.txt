lm-eval==0.4.9.2
huggingface-hub>=0.24,<1.0
numpy>=1.26,<3.0
pyyaml>=6.0,<7.0
sentencepiece>=0.1.99,<1.0
pytest>=8.0,<9.0
llama-cpp-python>=0.3.0,<1.0
# IFEval-specific extras (not pinned by lm-eval's base install)
langdetect>=1.0.9,<2.0
immutabledict>=4.0,<5.0
nltk>=3.9,<4.0
# Chat-template support: HF AutoTokenizer is loaded in parallel to the GGUF
# (prompt formatting only — never inference) so apply_chat_template() reads
# the canonical default chat template from each model's HF repo. Replicates
# Kurt 2026, §3.1.
transformers>=4.45,<5.0
tokenizers>=0.20,<1.0
jinja2>=3.1,<4.0
# protobuf: required by transformers' SentencePiece path for Mistral-v0.3's
# tokenizer (slow tokenizer fallback). Llama-3.1 (BPE) and Qwen3 (BPE) don't
# need it, but Mistral does. Without it, AutoTokenizer.from_pretrained(mistral)
# raises ImportError mid-run.
protobuf>=4.25,<6.0
